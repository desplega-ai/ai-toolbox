# cc-hooks

Claude Code hooks for macOS notifications.

## Setup

```bash
python setup.py
```

This adds notification hooks to `~/.claude/settings.json` for permission prompts, idle prompts, and elicitation dialogs.
